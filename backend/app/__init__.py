"""Cadence backend package."""

# Server-side version (backend + frontend + compose stack move together).
# Surfaced by FastAPI in the OpenAPI schema and /docs. The agent is versioned
# independently -- see docs/decisions.md "Versioning".
__version__ = "0.2.0"
