# Normalise line endings across platforms.
* text=auto eol=lf
*.png binary
*.ico binary

# Keep repo-meta files out of release archives (`git archive`).
/.github/ export-ignore
/.gitattributes export-ignore
/.gitignore export-ignore
