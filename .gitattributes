# Normalise line endings across platforms.
* text=auto eol=lf
*.png binary
*.ico binary

# Keep tooling / repo-meta files out of release archives (`git archive`).
/CLAUDE.md export-ignore
/.github/ export-ignore
/.gitattributes export-ignore
/.gitignore export-ignore
